<%= form_tag({:action => 'upload'}, :multipart => true, :method => :post) do %>
    <p><label>Select the data file (xml)</label><%= file_field_tag 'xml_file' %>
    <%= submit_tag "Upload" %>
 <% end %>